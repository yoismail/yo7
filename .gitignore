__pycache__/
*.pyc
.claude/

# Supabase CLI's own local cache/metadata (project ref, tool versions,
# pooler URL) — regenerated on every `supabase` command, not part of the
# project itself. Got committed once by accident via `supabase db dump`;
# ignoring it here so that doesn't happen again.
supabase/.temp/
